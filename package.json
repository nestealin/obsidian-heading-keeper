{
  "name": "heading-keeper",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@11.15.0",
  "engines": {
    "node": ">=22.20.0 <25"
  },
  "scripts": {
    "build": "corepack pnpm -r build",
    "format:check": "prettier --check .",
    "typecheck": "corepack pnpm -r build && corepack pnpm -r typecheck && tsc -p scripts/tsconfig.json",
    "test": "corepack pnpm -r build && vitest --config vitest.workspace.ts run",
    "package:plugin": "node scripts/package-plugin.mjs",
    "scan:sensitive": "node scripts/scan-sensitive.mjs",
    "scan:public-history": "node scripts/scan-public-history.mjs",
    "scan:public-surface": "node scripts/scan-public-surface.mjs",
    "scan:rejected": "node scripts/scan-rejected.mjs",
    "scan:word-joiner": "node scripts/scan-word-joiner.mjs",
    "verify:identity": "node scripts/verify-identity.mjs",
    "verify:deployment": "node scripts/verify-deployment.mjs",
    "verify:release": "corepack pnpm package:plugin && corepack pnpm scan:word-joiner && corepack pnpm scan:sensitive && corepack pnpm scan:public-surface && corepack pnpm scan:public-history && corepack pnpm verify:identity && node scripts/verify-release-rejected.mjs && vitest --config vitest.workspace.ts run scripts/test/release-surface.test.ts",
    "verify:local": "corepack pnpm format:check && corepack pnpm typecheck && corepack pnpm test && corepack pnpm build && corepack pnpm scan:sensitive && corepack pnpm scan:word-joiner && corepack pnpm scan:public-surface && corepack pnpm scan:public-history && corepack pnpm verify:identity && git diff --check"
  },
  "devDependencies": {
    "@types/node": "22.15.3",
    "fast-check": "4.9.0",
    "prettier": "3.6.2",
    "typescript": "5.9.2",
    "vitest": "4.0.16"
  }
}
